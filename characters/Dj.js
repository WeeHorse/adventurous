import Npc from "../engine/concepts/npc.js"

export default class Dj extends Npc{
    say(words){
        if(words.contains("change song"){
            // then whatever
            // this.useWeapon()
        }
    }
}